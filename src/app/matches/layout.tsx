import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Your Matches - Soulmate Sync | AI-Curated Compatible Profiles',
  description: 'View your daily AI-curated matches, mutual interests, and shortlisted profiles. Smart compatibility scoring for better connections.',
}

export default function MatchesLayout({ children }: { children: React.ReactNode }) {
  return children
}
