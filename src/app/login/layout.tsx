import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Login - Soulmate Sync | Access Your Matrimony Account',
  description: 'Login to your Soulmate Sync account to find matches, chat with profiles, and manage your matrimony journey.',
}

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children
}
