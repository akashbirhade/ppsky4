import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Search Profiles - Soulmate Sync | Find Your Perfect Match',
  description: 'Search verified matrimonial profiles by religion, city, education, age and more. AI-powered matchmaking for serious relationships.',
}

export default function SearchLayout({ children }: { children: React.ReactNode }) {
  return children
}
